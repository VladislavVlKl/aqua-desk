-- Раунд 2 сверки списаний: тренер подтверждает/исправляет число долговых занятий
-- прямо в карточке клиента (рядом с пакетным остатком), а не отдельно у координатора.
--   debt_shown — сколько незакрытого долга показали (снимок на момент опроса)
--   debt_final — сколько подтвердил тренер (исправленное число)
alter table pt_sequence_survey
  add column if not exists debt_shown int,
  add column if not exists debt_final int;
