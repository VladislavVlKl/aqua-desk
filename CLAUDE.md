# AquaDesk — Паспорт проекта

## Что это

Telegram Web App для управления фитнес-центром с бассейном.
Тренеры и координаторы ведут клиентов, тренировки, группы, дежурства и расчёт зарплаты прямо в Telegram.

---

## Документация

| Файл | Когда читать |
|---|---|
| [ARCHITECTURE.md](ARCHITECTURE.md) | Структура кода: файлы, навигация, карта секций app.js, паттерны, панели по ролям |
| [DATABASE.md](DATABASE.md) | Схема БД: таблицы, **FK-каскады (читать перед любым DELETE!)**, RPC, конвенции |

> **Правила:** изменил схему БД (миграция) → обнови DATABASE.md и положи SQL в `supabase/migrations/` в том же коммите. Существенно изменил структуру кода → обнови ARCHITECTURE.md.

---

## Доступы

| Сервис    | Данные                                                              |
|-----------|---------------------------------------------------------------------|
| GitHub    | https://github.com/VladislavVlKl/aqua-desk                         |
| Supabase  | Project ID: `nkwfvuhtpaoxsaczwsrg`, регион: eu-central-1           |
| Хостинг   | GitHub Pages (автодеплой при пуше в main)                           |

---

## Стек

- **Frontend**: Vanilla JS (ES2020), без фреймворков. UI разбит на 6 модулей `frontend/js/app*.js` + слой Supabase `frontend/js/db.*.js` (8 доменных файлов: core/clients/groups/schedule/analytics/ops/salary/misc). Порядок загрузки в `index.html` критичен (см. ARCHITECTURE.md)
- **Backend**: Supabase (PostgreSQL + RLS + RPC)
- **Деплой**: GitHub Pages, скилл `aquadesk-deploy`
- **Вход**: Telegram Web App (`window.Telegram.WebApp.initDataUnsafe.user.id`)

---

## Роли

| Роль             | Панель          | Доступ                                              |
|------------------|-----------------|-----------------------------------------------------|
| `trainer`        | Тренер          | Свои клиенты, тренировки, дежурства, конспекты      |
| `senior_trainer` | Старший тренер  | + группы, замены, подтверждение удалений            |
| `admin`          | Координатор     | Всё + персонал, расписание, расчёт зарплаты         |
| `ceo`            | Топ-менеджмент  | Дашборд, аналитика, ЗП сводка, группы, операционка, планы |
| `reception`      | Ресепшн         | Подтверждение списаний ПТ своего филиала (Шаг 1 → 1С), оплаты групп |
| `manager`        | Управляющий     | Один филиал, **только просмотр**: аналитика, персонал, группы, техчасть, ЗП поимённо, экспорт Excel |

Одна роль = одна панель. Мультипанель убрана.

---

## Ключевые бизнес-правила

### Тарифы (config.js)
```
Кат.1 = 85 000 сум/ПТ
Кат.2 = 110 000 сум/ПТ
Кат.3 = 130 000 сум/ПТ
Дежурство = 14 000 сум/час
Группы взрослые: до 3 чел=110к, до 6=120к, 7+=130к
Группы дети: 40% от оплат клиентов (group_children_pct)
PT_PRICES — клиентские цены ПТ для расчётной выручки CEO-аналитики
  (пока = тарифам тренеров, ⚠️ уточнить по реальному прайсу)
```

### Пакеты абонементов (SUB_PACKAGES)
```
Дети:     5 ПТ/2 нед | 10 ПТ/1 мес | 25 ПТ/3 мес | 50 ПТ/6 мес | Викенд 5 ПТ/1 мес (только сб/вс)
Взрослые: 5 ПТ | 10 ПТ | 25 ПТ — бессрочные (ПТ не сгорают, subscription_end не выставляется)
```
> Пакет «Викенд» (детский): `is_weekend` на clients+subscriptions; списание ПТ в будни жёстко заблокировано (`isWeekendDate` в config.js).

### Разовые посещения
Три варианта: dropin1 (кат.1), dropin2 (кат.2), dropin3 (кат.3).
Дети могут использовать разовое только 1 раз.

### Лимиты дат
- Тренировку можно внести за последние **72 часа** (MAX_BACKDATE_HOURS), позже — запрос на одобрение
- Конспект — дедлайн **48 часов** после тренировки (NOTE_DEADLINE_HOURS)
- Редактирование/удаление записи — **30 минут** (EDIT_WINDOW_MIN), позже — запрос на одобрение

### Критичные правила работы с данными
- Профили сотрудников **не удалять** — только архив (CASCADE снесёт их клиентов, см. DATABASE.md)
- Pending-запросы (удаление клиента/ПТ) защищены от дублей на уровне db.js
- Критичные операции оборачивать в `once(key, fn)` / `_pending` Set
- Кеш: `cached(key, fn, ttl=300000)`, сбрасывать `invalidateCache('profiles')` после записи

---

## Беклог

> **Правило:** при выполнении задачи — удалять строку из таблицы и коммитить CLAUDE.md вместе с кодом.

### Направления (крупные векторы развития)
1. Пушить тренеров к использованию — повысить вовлечённость, напоминания, онбординг
2. Интеграция с 1С — Step 1 (подтверждение списаний через панель «Ресепшн») реализован. Далее: Step 2/3 (двусторонняя сверка / прямой API 1С)
3. Клиентский опыт — детали не конкретизированы
4. Договор / ТЗ — формализация
5. Staging-среда — git ветка `staging` + Supabase Branch + Netlify для preview URL ($0.013/час, только пока активна)
6. Система онбординга — для новых тренеров и координаторов
7. Клиентская панель — отдельный интерфейс для клиентов фитнес-центра

### Задачи

| # | Задача | Статус |
|---|--------|--------|
| 1 | Push-уведомления через Telegram Bot API — **доставка переведена на pg_cron** (2026-09-07): GitHub Actions крон исполнялся раз в 6–21 ч вместо 5 мин (ср. задержка 117 мин). Теперь `pg_cron` job `process-notif-queue` раз в минуту → `pg_net` → Edge Function `supabase/functions/process-queue` → Telegram-чат; ретраи до 5 (`notifications_queue.attempts`), реальный текст ошибки в `error_text`. GH Actions крон отключён (ручной аварийный канал, `backend/jobs/process-queue.js` синхронизирован). **Вайтлист сейчас только «замены»** (`substitution`, `substitution_approve`); прочие события уходят в `status='skipped'` (колокольчик в приложении показывает их по-прежнему). Осталось: (а) поэтапно добавить в вайтлист остальные события (передача клиента, пересчёт категории, отклонение ресепшном, запросы на удаление/позднюю ПТ → координатору и решения → тренеру, одобрение/отклонение замены → обоим); менять массив `WHITELIST` в Edge Function + `process-queue.js` синхронно | Частично (доставка починена, вайтлист = замены) |
| 2 | Google Sheets выгрузка — нужны шаги настройки (Service Account + секреты) + Edge Function | Готов к реализации |
| 15 | Импорт клиентов из Excel — загрузка файла координатором, привязка к тренеру по фамилии | Готов к реализации |
| 9 | CEO панель — расширение/доработка | Частично: вкладка «Аналитика» сделана |
| 12 | Панель топ-менеджеров других направлений | На рассмотрении |
| 16 | Обеденное дежурство «лайт» — отдельный/упрощённый тип дежурства | Требует уточнения |
| 17 | Подгруппы «Арт-свим» — откорректировать логику появления подгрупп | Требует уточнения |
| 18 | Переименование группы — тренер и координатор могут менять название своей группы | Запланировано |
| 19 | Уведомления авто-триггеры — `notification_rules` есть в БД, но не работает (просроченный абонемент, нет конспекта и т.д.) | Запланировано |
| 20 | Проверить экспорт филиала — после всех правок убедиться, что работает корректно | Запланировано |
| 21 | Ресепшн: ручная корректировка общего остатка (зал+бассейн) — ресепшн помечает заловые списания, чтобы реальный остаток был меньше показанного. Доп. канал к тренерской кнопке «Списать в ТЗ» (db.js `deductGymSessions`) | Запланировано |
| 22 | Взрослые: заморозка/разморозка блока ПТ при членстве. Модель: абонемент (членство) и блок ПТ независимы; членство тренер ставит сам, блок ПТ к дате не привязан (уже сделано — `subscription_end` у взрослых не выводится из ПТ-пакета, мягкий режим). Доделать: (а) отдельное поле «Абонемент до» в добавлении/редактировании взрослого; (б) **жёсткая** заморозка — членство истекло → списание ПТ блокируется с подсказкой «продлите абонемент», продлили → разблокировалось; (в) **учёт по количеству дней** — замораживать остаток дней членства и восстанавливать при продлении (как freeze_start/freeze_end, но привязка к дням). ⚠️ Жёсткий режим включать только ПОСЛЕ (а), иначе у взрослых членство пустое → заблокирует все ПТ. Разовый сброс `subscription_end` у существующих взрослых — **сделано** (15 клиентов, 2026-07-06); взрослые пакеты теперь бессрочные и в UI (SUB_PACKAGES, превью «Действует до») | Запланировано |
| 23 | Координатор подтверждает списания за ресепшн — **сделано** (кнопки «✓ Подтвердить» в хабе «Контроль»: ПТ/разовые/долг/замены через `getReceptionHanging`+`confirmWorkout`, пробные через `getReceptionHangingTrials`+`confirmTrial`). ⚠️ Долг по API-режиму: `getReceptionHangingTrials` в `useApi('reception')` отдаёт `[]` — при флипе на FastAPI висящие **пробные** не подтянутся в «Контроль», пока на бэкенде не появится эндпоинт `/reception/hanging` для `trial_sessions` (ПТ/разовые работают в обоих режимах) | Частично (API-режим) |
| 24 | Опросник сверки порядковых списаний ПТ (Chekhov Moms). **Раунд 1** (`round='2026-09'`) собран: 85 ответов/69 активных. **Выравнивание сделано** — 22 чистых активных клиента поправлены прямым `UPDATE clients.balance` с логом `audit_log action='seq_align'` (before→after, откатываемо); цель = `final_total−(final_next−1)−(не-долговые списания после ответа)`; долг НЕ вычитается из остатка. Пропущены: 39 «уже совпадало», 6 крупных дельт, 2 долга, 16 архивных. **Раунд 2** (`round='2026-09-verify'`, дедлайн 12.09) **запущен**: verify-текст баннера, у выравненных бейдж «поправлено» (по `audit_log seq_align`), у долговых редактируемое поле «В долге, шт» (`pt_sequence_survey.debt_shown/debt_final`, миграция 20260909120000). Напоминания `remind.js` `ruleSeqSurvey` (10.09 10/18, 11.09 10, 12.09 10). Гейт `seqSurveyEnabled`+`SEQ_SURVEY` (config.js). Supabase-only. **Осталось:** (а) по итогам раунда 2 применить подтверждённые правки (та же логика выравнивания + учесть debt_final); (б) далее — подключение ресепшена (Step 2 интеграции с 1С). | Частично (раунд 2 идёт) |

### На рассмотрении
- Step 3 интеграции с 1С (прямой API) — нужно выяснить тип 1С и наличие разработчика
- Telegram-бот в группу + Google Sheet (вариант 2) — бот читает сообщения тренеров, парсит через Claude API (~$20–40/мес) и пишет в Google Sheet. Хостинг Railway/Render (free tier / от $5/мес), Sheets API и бот бесплатно. Риск: неоднозначные сообщения → бот может внести не то / не туда (чувствительно для детских групп)

---

## Как передать контекст новой сессии

1. Этот файл (`CLAUDE.md`) — паспорт и беклог
2. `ARCHITECTURE.md` / `DATABASE.md` — по необходимости
3. `git log --oneline -10` — последние коммиты

Рабочая директория: `/Users/vladislavklimov/aqua-desk`
